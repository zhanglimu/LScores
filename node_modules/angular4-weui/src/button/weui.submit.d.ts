export declare class WeUISubmit {
    /**
     * 颜色，取值：default、primary、warn等。默认为default。<br>
     * 自定义的颜色名称与色值，可以定义在 工程根目录/src/theme/variables.scss 文件中的 $colors 对象。
     */
    color: string;
    /** 按钮类型 */
    type: string;
    /**
     * 简朴样式 (主要出现此属性)
     */
    plain: string;
    /**
     * 按钮大小 (主要出现此属性)
     */
    mini: string;
    /**
     * 正在加载
     */
    loading: boolean;
    /**
     * 禁用样式
     */
    disabled: boolean;
    constructor();
    getColorStyle(): string;
}
