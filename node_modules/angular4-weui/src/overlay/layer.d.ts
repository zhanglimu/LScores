import { ComponentFactoryResolver, ApplicationRef, Injector, NgZone } from '@angular/core';
import { WeUIToast } from '../toast/weui.toast';
export interface DialogProps {
    mode?: string;
    title?: string;
    content?: string;
    btnNOText?: string;
    btnOKText?: string;
    showNOButton?: boolean;
    type?: string | null;
    icon?: string;
    iconCls?: string;
}
/**
 * 浮层通用接口
 */
export declare class Layer {
    private _componentFactoryResolver;
    private _appRef;
    private _injector;
    private _ngZone;
    constructor(_componentFactoryResolver: ComponentFactoryResolver, _appRef: ApplicationRef, _injector: Injector, _ngZone: NgZone);
    /**
     * Creates the DOM element for an overlay and appends it to the document.
     * @returns Newly-created pane element
     */
    private _createDivElement();
    private _createCompInstance<T>(component);
    private _createDialog();
    /**
     * 显示Success
     *
     * @param message  消息
     * @param title    标题(可选)
     */
    success(message: string, title?: string): Promise<any>;
    /**
     * 显示Info
     *
     * @param message  消息
     * @param title    标题(可选)
     */
    info(message: string, title?: string): Promise<any>;
    /**
     * 显示Error
     *
     * @param message  消息
     * @param title    标题(可选)
     */
    error(message: string, title?: string): Promise<any>;
    /**
     * 显示Warning
     *
     * @param message  消息
     * @param title    标题(可选)
     */
    warning(message: string, title?: string): Promise<any>;
    /**
     * 显示Alert
     *
     * @param message  消息
     * @param title    标题(可选)
     * @param type     告警等级
     */
    showAlert(message: string | DialogProps, title?: string, type?: string): Promise<any>;
    /**
     * 显示Confirm，默认带两个按钮（取消、确定）
     *
     * @param message  消息，或者自定义对话框属性对象
     * @param title    标题
     */
    showConfirm(message: string | DialogProps, title?: string): Promise<any>;
    private _createToast();
    /**
     * 显示Loading Toast
     *
     * @param message  消息，默认为“数据加载中”
     */
    showLoading(message?: string): WeUIToast;
    /**
     * 显示成功Success Toast
     *
     * @param message  消息，默认为“操作成功”
     */
    showSuccess(message?: string): WeUIToast;
    /**
     * 在顶部显示错误消息
     *
     * @param message  错误消息
     */
    showError(message: string): void;
    private _createActionSheet();
    /**
     * 显示ActionSheet菜单
     *
     * @param buttons 菜单按钮
     * @param btnCancelText 取消按钮文本，默认为“取消”
     */
    showActionsheet(menus: {
        text?: string;
        [key: string]: any;
    }[], btnCancelText?: string): Promise<any>;
    /** Gets the root HTMLElement for an instantiated component. */
    private _getComponentRootNode(componentRef);
}
