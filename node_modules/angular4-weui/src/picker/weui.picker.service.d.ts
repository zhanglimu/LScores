import { ComponentFactoryResolver, ApplicationRef, Injector, NgZone } from '@angular/core';
import { PickerOption } from './weui.picker';
/**
 * Picker服务
 */
export declare class WeUIPickerService {
    private _componentFactoryResolver;
    private _appRef;
    private _injector;
    private _ngZone;
    private doc;
    constructor(_componentFactoryResolver: ComponentFactoryResolver, _appRef: ApplicationRef, _injector: Injector, _ngZone: NgZone, doc: Document);
    /**
     * 显示单列Picker
     *
     * @param menu  菜单选项
     */
    showPicker(menu: PickerOption[]): Promise<any>;
    /**
     * 显示多列Picker
     *
     * @param menus  菜单选项（多列）
     */
    showMultiPicker(menus: PickerOption[][]): Promise<any>;
    /**
     * 显示日期Picker
     *
     * @param config  配置项
     */
    showDatePicker(config?: any): Promise<any>;
    private createPicker(component, config);
    /** this method is called before thild component's ngAfterViewInit */
    protected afterCreate(instance: any): void;
    private _createComponent<T>(component);
    /** Gets the root HTMLElement for an instantiated component. */
    private _getComponentRootNode(componentRef);
    private _getContainer();
}
