/**
 * @license
 * Copyright 厦门乾元盛世科技有限公司 All Rights Reserved.
 *
 * Use of this source code is governed by an MIT-style license that can be
 * found in the LICENSE file.
 */
import { OnInit, AfterViewInit, QueryList } from '@angular/core';
import { WeUIPicker, PickerOption } from './weui.picker';
import { WeUIPickerGroup } from './weui-picker-group';
/**
 * 日期选择器
 */
export declare class WeUIDatePicker extends WeUIPicker implements OnInit, AfterViewInit {
    /**
     * cron 表达式，三位，分别是 dayOfMonth[1-31]，month[1-12] 和 dayOfWeek[0-6]（周日-周六）。<br>
     * 格式: 日期 月份 周。例如：* * * 或 1,2,5-9 1-6 0-6 或 1/2 * * 等。默认：* * *。
     */
    cron: string;
    /**
     * 开始日期/年份。取值：<ul>
     * <li>(1)字符串：符合ISO 8601日期格式的字符串，如：2013-01-18 或者 20130118；</li>
     * <li>(2)数值：表示年份，如：2011，则默认加"-01-01"在年份之后 </li>
     * <li>(3)Date：JavaScript Date对象 </li>
     * </ul>
     */
    start: any;
    /**
     * 结束日期/年份。取值：<ul>
     * <li>(1)字符串：符合ISO 8601日期格式的字符串，如：2013-01-18 或者 20130118；</li>
     * <li>(2)数值：表示年份，如：2011，则默认加"-12-31"在年份之后 </li>
     * <li>(3)Date：JavaScript Date对象 </li>
     * </ul>
     */
    end: any;
    private _startDate;
    private _endDate;
    /** 显示列表 */
    _groups: QueryList<WeUIPickerGroup>;
    /** 月份选择控件 */
    private monthPicker;
    /** 日期选择控件 */
    private datePicker;
    private _parseDate(value, sub);
    constructor();
    ngOnInit(): void;
    ngAfterViewInit(): void;
    /**
     * 选择年或月，触发子列表的变更
     *
     * @param option 被选择的选项
     * @param index 哪个列（索引）
     */
    onChange(option: PickerOption, index: number): void;
    private _updateMonthPickerList();
    private _updateDatePickerList();
}
